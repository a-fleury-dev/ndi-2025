spring:
  application:
    name: chat-api

openai:
  api-key: ${OPENAI_API_KEY}
  org-id: ${OPENAI_ORG_ID}
  project-id: ${OPENAI_PROJECT_ID}
  model: gpt-3.5-turbo
  temperature: 0.7
  max-tokens: 1000

frontend:
  url: ${FRONTEND_URL}

